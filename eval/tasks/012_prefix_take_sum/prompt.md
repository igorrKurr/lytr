# Task 012 — Prefix take, then sum

Use `take` on a `range` stream so only the first elements feed `reduce sum`. `starter.lir` must pass `check`, `run` (empty input), `fmt --check`, and `codegen-check`.
