# Task 018 — Bounded head, count evens

Take a bounded prefix of a large range, keep only even values, then count them. `starter.lir` must pass `check`, `run` (empty input), `fmt --check`, and `codegen-check`.
