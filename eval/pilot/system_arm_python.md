# Output: Python 3

- One complete script; **standard library only** (no pip packages).
- **Stdout:** print exactly one line — the decimal integer answer and a newline. No other printing.
- Implement the reference pipeline’s semantics; do **not** shell out to `lir`.
